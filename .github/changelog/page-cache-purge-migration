Significance: patch
Type: added

Add the 0.35.4 migration that clears page caches. GatherPress 0.35.4 changes the markup the RSVP response list refreshes from, so pages cached before the update stop refreshing their response lists until the cache is cleared. The migration clears the page cache of supported caching plugins and hosts when they are active and flushes the object cache.
